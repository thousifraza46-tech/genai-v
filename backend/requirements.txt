# Production Backend Requirements (Lightweight)
# For full local development, use ../requirements.txt in root

# Core Web Framework
flask==3.0.0
flask-cors==4.0.0
waitress==3.0.0

# HTTP & Networking
requests==2.32.0
urllib3==2.0.0

# Video & Image Processing (Essential only)
moviepy==1.0.3
imageio==2.31.3
imageio-ffmpeg==0.4.9
pillow==10.0.0
numpy==1.24.3

# AI Services
google-generativeai==0.3.2
google-auth==2.23.0

# Audio Generation
edge-tts==6.1.9

# Utilities
python-dotenv==1.0.0
nltk==3.8.1
tqdm==4.66.1
typing-extensions==4.8.0

# Note: Heavy dependencies removed for production deployment
# - torch, transformers, diffusers (AnimateDiff - requires GPU)
# - opencv-python (optional, large file)
# - replicate (optional, only if using paid API)
